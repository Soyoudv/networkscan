# networkscan
doing a network scan in bash because its fun idk

clone the git repo:
```sh
git clone https://github.com/Soyoudv/networkscan.git && cd networkscan
```

launch the program:
```sh
./nwscan.sh
```